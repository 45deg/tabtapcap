{
  "name": "local-tab-transcriber",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "dev": "concurrently -k -n server,viewer,extension -c blue,green,magenta \"pnpm dev:server\" \"pnpm --filter viewer dev\" \"pnpm --filter extension dev\"",
    "dev:server": "cargo run --manifest-path server/Cargo.toml",
    "tauri:dev": "pnpm --filter viewer tauri:dev",
    "tauri:build": "pnpm --filter viewer tauri:build",
    "build": "pnpm --filter viewer build && pnpm --filter extension build",
    "test": "pnpm -r test && cargo test --manifest-path server/Cargo.toml",
    "check": "pnpm -r check && cargo fmt --manifest-path server/Cargo.toml --check && cargo clippy --manifest-path server/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path server/Cargo.toml"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
