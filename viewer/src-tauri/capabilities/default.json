{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Local Tab Transcriber window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
